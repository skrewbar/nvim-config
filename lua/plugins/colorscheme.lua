-- Configure the default colorscheme via LazyVim plugin opts
-- This lets LazyVim handle applying the colorscheme at startup
return {
  {
    "LazyVim/LazyVim",
    opts = {
      colorscheme = "pastel-pink",
    },
  },
}
